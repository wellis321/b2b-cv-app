// The load function - data is loaded client-side
// with the cvStore. This is just a minimal server-side load function.
export const load = async ({ locals }) => {
	// The actual data loading happens in +page.ts client-side
	return {};
};
